# TODO

- [ ] Implement Lenis/scroll-linked effects in `src/components/Hero.tsx`:
  - [ ] Add scroll progress tracking via Framer Motion `useScroll`.
  - [ ] Apply scroll-linked parallax to the background layer.
  - [ ] Apply scroll-linked modulation to the SVG displacement `scale`.
  - [ ] Respect `prefers-reduced-motion`.
  - [ ] Preserve existing ambient turbulence RAF + current entrance animations.
- [x] Run dev server and visually QA (no stutter / works with Lenis).

